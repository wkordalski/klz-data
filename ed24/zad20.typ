#import "template.typ": template
#show: template.with(deadline: [6 maja])

= Zadanie 20

Wykaż, że dla dowolnej liczby naturalnej $n >= 1$ istnieje liczba całkowita dodatnia zapisana (w~systemie dziesiętnym) tylko przy pomocy zer i jedynek, która dzieli się przez $n$.
